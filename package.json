{
  "type": "module",
  "scripts": {
    "tokens": "npx make-sugarcube generate",
    "css": "npx postcss src/css/*.css --dir dist/css",
    "clean": "rm -r -f dist && mkdir dist",
    "start": "npm run clean && npm run tokens && cp src/index.html dist/index.html && concurrently 'npx serve dist' 'npm run css -- --watch'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clean-css": "^5.3.2",
    "concurrently": "^8.2.2",
    "cssnano": "^6.0.1",
    "postcss": "^8.4.31",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0",
    "postcss-import-ext-glob": "^2.1.1"
  }
}
